import 'package:flutter/cupertino.dart';

class Test extends StatelessWidget {
  const Test({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        //dropdown dengan items, selectedvalue, onchanged yang bisa merubah selectedValue dari bloc
        //bloc builder menampilkan list product yang di filter dari dropdown
        //bloc builder menampilkan semua list product tanpa filter
      ],
    );
  }
}